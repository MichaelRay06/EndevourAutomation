declare type ID = string | undefined;
declare type Event = 'add' | 'change' | 'rename' | 'unlink';
declare type Stats = {
    [filePath: string]: import('fs').Stats;
};
declare type Locks = {
    [id: string]: Function;
};
declare type LockOptions = {
    locks: {
        read: Locks;
        write: Locks;
    };
    handlers: {
        free: Function;
        override: Function;
        overridden: Function;
    };
};
declare type Handlers = {
    add?: (filePath: string, stats: import('fs').Stats) => void;
    change?: (filePath: string, stats: import('fs').Stats) => void;
    rename?: (prevFilePath: string, nextFilePath: string) => void;
    unlink?: (filePath: string) => void;
};
export { ID, Event, Stats, Locks, LockOptions, Handlers };
